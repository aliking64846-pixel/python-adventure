const {createApp}=require('./app');const {initDb}=require('./db/init');const PORT=Number(process.env.PORT||10000);async function startServer(){try{await initDb();const app=createApp();app.listen(PORT,'0.0.0.0',()=>console.log(`Python Adventure running on port ${PORT}`));}catch(error){console.error('Server startup failed:',error);process.exit(1);}}if(require.main===module)startServer();module.exports={startServer};
