function escapeHtml(v){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function openChat(){$('chatModal').classList.add('show');$('chatSearch').focus()}
async function selectUser(u){selectedUser=u;$('chatTitle').textContent='💬 محادثة مع '+u.username;$('messageInput').disabled=false;$('sendBtn').disabled=false;await loadMessages()}
async function loadMessages(){if(!selectedUser)return;const d=await api('/messages/'+selectedUser.id);$('messages').innerHTML=(d.messages||[]).map(m=>`<div class="message ${m.sender_id===currentUser.id?'mine':'theirs'}">${escapeHtml(m.body)}</div>`).join('');$('messages').scrollTop=$('messages').scrollHeight}
async function sendMessage(){if(!selectedUser)return;const body=$('messageInput').value.trim();if(!body)return;try{await api('/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({receiverId:selectedUser.id,body})});$('messageInput').value='';await loadMessages()}catch(e){toast(e.message)}}
$('chatClose').onclick=()=>{$('chatModal').classList.remove('show')};$('chatModal').onclick=e=>{if(e.target===$('chatModal'))$('chatModal').classList.remove('show')};$('chatSearch').oninput=async()=>{const q=$('chatSearch').value.trim();if(!q){$('users').innerHTML='';return}try{const d=await api('/users?search='+encodeURIComponent(q));$('users').innerHTML=d.users.map(u=>`<div class="user" onclick='selectUser(${JSON.stringify(u)})'>🧙 ${u.username}<br><small>المستوى ${u.level}</small></div>`).join('')||'لا يوجد لاعب'}catch(e){}};$('sendBtn').onclick=sendMessage;$('messageInput').onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();sendMessage()}};setInterval(()=>{if(selectedUser&&$('chatModal').classList.contains('show'))loadMessages()},4000);
