function renderBag(){$('quickBag').innerHTML=items.slice(0,Math.min(items.length,3+completedLessons.length)).map(x=>`<button class="item" onclick="toast('${x}')">${x}</button>`).join('');$('bagList').innerHTML=items.map((x,i)=>`<button class="item ${i>2+completedLessons.length?'locked':''}" onclick="toast('${i>2+completedLessons.length?'🔒 أكمل المزيد من الدروس':x}')">${i>2+completedLessons.length?'🔒 ':''}${x}</button>`).join('');$('bagCount').textContent=Math.min(items.length,3+completedLessons.length)+'/30'}
